---
import '../styles/global.css';

interface Props {
  title: string;
  backUrl?: string;
}

const { title, backUrl = '/math-for-kids/' } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title} - Math Fun for Kids!</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/7.0.0/css/all.min.css">
    <link rel="icon" type="image/svg+xml" href="/math-for-kids/favicon.svg">
</head>
<body class="font-sans bg-gradient-to-br from-indigo-500 to-purple-700 min-h-screen">
    <div class="min-h-screen p-5">
        <div class="max-w-7xl mx-auto bg-white rounded-3xl p-8 shadow-2xl min-h-[calc(100vh-2.5rem)]">
            <!-- Header -->
            <div class="flex items-center gap-5 mb-8">
                <a href={backUrl} class="bg-gray-100 hover:bg-gray-200 text-gray-700 px-4 py-2 rounded-xl font-semibold transition-all hover:-translate-x-1 no-underline">
                    ← Back Home
                </a>
                <h1 class="text-gray-700 text-4xl font-black flex-1 text-center">{title}</h1>
            </div>

            <!-- Game Content -->
            <slot />
        </div>
    </div>

    <!-- Footer -->
    <footer class="py-8">
        <div class="max-w-7xl mx-auto px-5">
            <div class="bg-white/10 backdrop-blur-md rounded-3xl p-6 border border-white/20">
                <div class="text-center">
                    <p class="text-white text-lg mb-4 font-bold">Have fun learning math! 🌟</p>
                    <div class="flex justify-center items-center flex-wrap gap-3 text-white text-sm">
                        <div class="flex items-center gap-2">
                            <span>Made with ❤️ by</span>
                            <span class="font-bold text-yellow-300">Andrea & Defne</span>
                        </div>
                        <div class="text-white/50 font-bold">•</div>
                        <a href="/math-for-kids/games/changelog" class="text-white hover:text-yellow-300 transition-colors no-underline font-semibold">
                            📝 Changelog
                        </a>
                        <div class="text-white/50 font-bold">•</div>
                        <a href="mailto:andrea@techinterview.coach" class="text-white hover:text-yellow-300 transition-colors no-underline font-semibold">
                            📧 Contact
                        </a>
                        <div class="text-white/50 font-bold">•</div>
                        <a href="https://github.com/andreadellacorte/math-for-kids" target="_blank" class="text-white hover:text-yellow-300 transition-colors no-underline font-semibold">
                            <i class="fab fa-github"></i> GitHub
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </footer>
</body>
</html>
