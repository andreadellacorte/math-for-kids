# Run tests (deprecated during Astro migration)
npm test

# Run production build (now builds to dist/ which is gitignored)
npm run build:prod

# Note: dist/ is now built by GitHub Actions, not committed to git
